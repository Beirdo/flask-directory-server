# flask-directory-server
Using Flask to serve a simple directory (via nginx)

# Using

```
$ pip3 install virtualenv
$ virtualenv venv
$ source venv/bin/activate
$ pip3 install -r requirements.txt
$ src/server.py
```
